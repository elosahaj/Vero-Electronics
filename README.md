### This program simulates the work of an online electronics store and populates the local MySQL database (snowflake schema) with randomly generated orders. 
### Items have different properties and price ranges.
